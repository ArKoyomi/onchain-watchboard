import test from 'node:test';import assert from 'node:assert/strict';import {capInUSD,capInput,capInputK,entryTime} from '../lib/entry-fields.mjs';
test('automatic units choose K M B and preserve exact estimate on submission',()=>{assert.deepEqual(capInput(null),{value:'',unit:'K'});for(const [amount,unit] of [['999999','K'],['1000000','M'],['2392586.75158561','M'],['999999999','M'],['1000000000','B'],['2392586751.58561234','B'],['1.000000000001','K']]){const result=capInput(amount);assert.equal(result.unit,unit);assert.equal(capInUSD(result.value,result.unit),amount)}assert.equal(capInput('2392586.75158561').value,'2.39258675158561')});
test('market cap units retain decimal precision',()=>{assert.equal(capInUSD('123.45','K'),'123450');assert.equal(capInUSD('1.25','M'),'1250000');assert.equal(capInUSD('0.3','B'),'300000000');assert.equal(capInUSD('1.000000000001','K'),'1000.000000001');assert.equal(capInUSD('','K'),'');assert.throws(()=>capInUSD('0','K'));assert.throws(()=>capInUSD('1','T'));assert.equal(capInputK('123450.000000000000'),'123.45');assert.equal(capInputK('1'),'0.001')});
test('default time uses server now; custom time normalized to UTC',()=>{assert.equal(entryTime(''),null);assert.equal(entryTime('2026-09-01T12:00:00+08:00'),'2026-09-01T04:00:00.000Z');assert.throws(()=>entryTime('invalid'))});
