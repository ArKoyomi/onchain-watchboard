# 第一版验证记录

## 2026-09-10 GeckoTerminal 历史分钟估算

- 自动测试 24 项通过；003 迁移另外在 PGlite 中验证估算来源保存、公共读取和手动覆盖清理。类型检查、生产构建通过。
- 真实只读查询 Base WETH、Solana WSOL，均取得最近十分钟前的分钟估算。此样本不代表所有小币、历史范围和网络都覆盖。
- 003 增量迁移已通过 Supabase SQL Editor 执行，返回 Success。没有编辑或删除用户记录，没有写入测试币。
- 历史价格来源 GeckoTerminal，供应量以 DEX 当前市值/价格反推；不是真实历史供应量。自动预填只能使用已结束、有成交且接近目标时间的分钟线，不宣称秒级精度。
- 自定义时间下空白市值现在保存为空。用户手填优先，估算采用服务器签名绑定 CA、链、时间和金额；确认保存时保留采样依据。未进行浏览器视觉验收。

## 2026-09-10 收录编辑与删除升级

- 002 增量迁移通过真实 Supabase SQL Editor 执行，返回 Success；旧归档记录恢复到公共列表，没有删除用户记录。
- 自动测试 15/15 通过，覆盖 K/M/B 换算、自定义时间、重复提交、管理员编辑、成员拒绝、删除与收藏清理、重新添加。
- TypeScript 检查与生产构建通过。本次没有对真实用户记录执行编辑/删除测试，也没有进行浏览器视觉验收。
- 编辑、删除入口位于管理员每行末尾「…」，删除有二次确认；自定义收录时间不会自动匹配历史市值。

## 2026-09-10 真实项目连接验证

- 通过用户已登录的 Supabase SQL Editor 执行初始化事务，界面返回 Success。
- 创建管理员 `admin`，成员表 role=admin、enabled=true；密码仅保存在被忽略的本地文件。
- `node scripts/check-live-connection.mjs` 通过：公共列表版本0、0条记录；admin登录；刷新后仍为admin；退出后匿名；匿名添加返回401。
- 登录改为用户名/密码，使用内部占位邮箱，无邮件发送和真实邮箱要求。没有新增测试币或修改用户密码。
- 本地预览服务已重启并加载 `.dev.vars`。此前“没有真实项目”的状态已被本节取代；正式部署、手机访问和真实币收录仍未验收。

日期：2026-09-09。当前交付范围由用户明确为「先完成代码和配置指引」，不包含真实 Supabase 项目配置或正式发布。

已执行：

- `node --test tests/*.test.mjs`：10/10 通过。
- `node node_modules/typescript/bin/tsc --noEmit`：通过。
- `npm run build`：通过，生成 Worker 与前端资源；本机用 npm CLI 的完整路径执行，避免系统 npm shim 路径问题。
- 本地首页 HTTP 200，已将 http://localhost:5173/ 提交到 Codex 预览面板（工具返回 queued）。
- 实际 DEX 查询：Ethereum WETH、BSC WBNB、Base WETH、Solana WSOL 各一次，返回匹配行情且 marketCap 有值，样本保存在 outputs/live-coverage.json。这不代表全部 CA、行情准确性或延迟保证。
- PGlite 中执行完整 PostgreSQL 建表脚本；验证首次市值不可改、同链唯一与跨链独立、归档恢复、版本变化和收藏 RLS。Promise 并发在本地数据库中被串行调度，不等同于云端多连接负载测试；SQL 的唯一键与事务锁仍承担正确性。
- 后端 mock 测试证明持有有效列表许可的行情请求只调用 DEX，不访问 Supabase；篡改 CA、跨域写入被拒绝，429 返回退避信息。
- WebMCP 注册/验证使用模拟上下文通过；没有可用的真实 WebMCP 浏览器上下文验证，不能声称浏览器标准集成已验收。

待接入真实项目后验证：Supabase Auth 登录/刷新/密码修改、跨设备收藏、真实成员操作、生产 RLS、Cloudflare CPU 与用量、大陆网络、Robinhood 真实 CA、浏览器端交互与手机视觉验收、完整备份恢复。没有运行截图或浏览器自动操作测试；响应式代码和类型检查不等于视觉测试。

无云数据库时应用显示「尚未连接公共数据库」，不将模拟数据或 localStorage 作为公共记录。本站已登记但未发布。旧共享缓存实施文档已标注为历史版本。
